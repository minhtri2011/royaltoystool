import convertTextToBold from "../feature/convertTextToBold"

const template=(
    name,size,price,deposit,bankfull,limited,material, accessory, releaseDate,note
)=>{
    return`${convertTextToBold()}`
}
export default template